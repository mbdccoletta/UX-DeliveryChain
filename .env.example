# Copy to .env and point at your own Dynatrace environment.
# Overrides the placeholder in app.config.json; the --environment-url flag
# still wins over both.
DT_APP_ENVIRONMENT_URL=https://<your-environment>.apps.dynatrace.com
